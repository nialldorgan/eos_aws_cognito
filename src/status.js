const status = {
    login_succeeded: "Login succeeded",
    totp_required: "A TOTP is required",
    sms_mfa: "An SMS MFA code is required",
    newPassword: "A new password is required",
    customChallenge: "Custom auth challenge (contact your network administrator for further information if required)"
};

export {status};
export default status;

